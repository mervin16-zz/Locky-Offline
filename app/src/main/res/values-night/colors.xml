<?xml version="1.0" encoding="utf-8"?>

<resources>

    <!-- =================== -->
    <!-- Default App Colors -->
    <!-- =================== -->
    <color name="colorPrimary">@color/dark_colorAccent</color>
    <color name="colorPrimaryDark">@color/dark_colorPrimaryDark</color>
    <color name="colorAccent">@color/dark_colorAccent</color>
    <color name="colorOnSurface">@color/dark_colorOnSurface</color>
    <color name="colorShadowColor">@color/grey_100</color>
    <color name="dark_colorOnSecondary">@color/grey_dark_100</color>
    <!--Nav bar color protection for behavior differences between Android P and Q. All sdk-->
    <!--levels should draw fullscreen. P should color the nav bar with a translucent background -->
    <!--to guard for contrast. Q should have a transparent nav bar as Q will automatically -->
    <!--handles contrast by either dynamically coloring the nav bar handle (gesture nav) -->
    <!--or automatically applying a translucent background (3-button mode).-->
    <color name="colorNavigationBar">@color/colorPrimaryDark</color>

    <!-- ============= -->
    <!-- Button Colors -->
    <!-- ============= -->
    <color name="colorFabNormal">@color/dark_colorOnSecondary</color>
    <color name="colorFabMini">@color/dark_colorOnSecondary</color>

    <!-- ================= -->
    <!-- Text Field Colors -->
    <!-- ================= -->
    <color name="colorTextFieldAccentSearch">@color/colorAccent</color>
    <color name="colorTextFieldTextDefault">@color/colorAccent</color>

    <!-- =================== -->
    <!-- Iconography Colors -->
    <!-- =================== -->
    <color name="colorIcon">@color/grey_100</color>
    <color name="colorIconSurface">@android:color/white</color>
    <color name="colorIconFabMini">@android:color/white</color>
    <color name="colorIconFabNormal">@color/colorAccent</color>

    <!-- ===================== -->
    <!-- Material Cards Colors -->
    <!-- ===================== -->
    <color name="colorCardBackground">@color/dark_colorOnSecondary</color>

    <!-- ================= -->
    <!-- Typography Colors -->
    <!-- ================= -->
    <color name="colorTextTitleToolbar">@color/colorAccent</color>
    <color name="colorTextTitle">@color/grey_50</color>
    <color name="colorTextSubtitle">@color/grey_200</color>
    <color name="colorTextBody">@color/grey_200</color>
    <color name="colorTextMenu">@color/grey_200</color>
    <color name="colorButtonTextAccentDark">@color/colorAccent</color>

</resources>